import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vlille-finder',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
